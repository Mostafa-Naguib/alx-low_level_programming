#include "main.h"


/**
 * _isdigit - checks for a digit (0 through 9).
 *
 * Description: checks for a digit (0 through 9).
 * @c: The character
 *
 * Return: 1 (Success)
 */


int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}

